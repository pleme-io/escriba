[package]
name = "escriba-spec"
description = "Generated OpenAPI 3.1 specs — committed in-tree, regenerated by escriba-api, gated by CI. The editor's public-API source of truth."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
escriba-api = { workspace = true }

[[bin]]
name = "escriba-spec-dump"
path = "src/bin/dump.rs"

[dev-dependencies]
pretty_assertions = { workspace = true }
serde_json        = { workspace = true }

[lints]
workspace = true
